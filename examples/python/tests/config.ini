# This file is for example only. It is not actually required if you're ok
# with default config so you can safely delete it.

[CONFIG]
FILTERS = DIGITS_ONLY
TESTCASE_WEIGHTS = test_output.py:0.5, test_result.py:1